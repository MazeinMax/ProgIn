# Тема 8. Введение в ООП.
Отчет по Теме #8 выполнила:
- Мазеин Максим Сегреевич
- АИС-23-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

## Лабораторная работа №1
### Создайте класс “Car” с атрибутами производитель и модель. Создайте объект этого класса. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями.

```python
# Определяем класс Car
class Car:
    # Конструктор класса Car
    def __init__(self, make, model):
        # Инициализируем атрибуты make и model
        self.make = make
        self.model = model

# Создаем объект my_car типа Car
my_car = Car("Toyota", "Corolla")
```

### Результат.
<img width="614" height="379" alt="image" src="https://github.com/user-attachments/assets/30641273-6be4-4f6a-ae39-bd6a0c784b6e" />

### Выводы.
1. Определен класс Car с двумя атрибутами: make и model.
2. Конструктор init() инициализирует эти атрибуты при создании объекта.
3. Метод drive() выводит информацию о машине, используя оба атрибута.

## Лабораторная работа №2
### Дополните код из первого задания, добавив в него атрибуты и методы класса, заставьте машину “поехать”. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями и получившийся вывод в консоль.

```python
# Определяем класс Car
class Car:
    # Конструктор класса Car
    def __init__(self, make, model):
        # Инициализируем make и model
        self.make = make
        self.model = model

    def drive(self):
        print(f"Driving the {self.make} {self.model}")
# Создаем объект my_car типа Car
my_car = Car("Toyota", "Corolla") # Создание нового экземпляра класса Car
my_car.drive()
```

### Результат.
<img width="777" height="539" alt="image" src="https://github.com/user-attachments/assets/90375ae7-b155-4ba9-a62e-ce9acf894256" />
<img width="524" height="70" alt="image" src="https://github.com/user-attachments/assets/6f158272-3d79-493b-879e-51ff9bc09f87" />

### Выводы
Класс обеспечивает инкапсуляцию защищенного атрибута _make и приватного атрибута __model.

## Лабораторная работа №3
### Создайте новый класс “ElectricCar” с методом “charge” и атрибутом емкость батареи. Реализуйте его наследование от класса, созданного в первом задании. Заставьте машину поехать, а потом заряжаться. Михаил А. Панов Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями и получившийся вывод в консоль.

```python
# Определяем класс Car
class Car:
    # Конструктор класса Car
    def __init__(self, make, model):
        # Инициализируем make и model
        self.make = make
        self.model = model

    def drive(self):
        print(f"Driving the {self.make} {self.model}")
# Создаем объект my_car типа Car
my_car = Car("Toyota", "Corolla") # Создание нового экземпляра класса Car
my_car.drive()

# Определяем подкласс ElectricCar, наследующий от Car
class ElectricCar(Car):
    # Конструктор ElectricCar с дополнительным параметром battery_capacity
    def __init__(self, make, model, battery_capacity):
        # Вызываем конструктор базового класса Car
        super().__init__(make, model)
        self.battery_capacity = battery_capacity

    # Метод для заряда батареи электромобиля
    def charge(self):
        print(f"Charging the {self.make} {self.model} with {self.battery_capacity} kWh")

# Создаем объект my_electric_car типа ElectricCar
my_electric_car = ElectricCar("Tesla", "Model S", 75)

# Вызываем методы для электромобиля
my_electric_car.drive()
my_electric_car.charge()
```

### Результат.
<img width="848" height="722" alt="image" src="https://github.com/user-attachments/assets/0a414a7f-9cc6-4b87-b037-537f45392287" />
<img width="1330" height="261" alt="image" src="https://github.com/user-attachments/assets/cf06ea1f-5ca3-4391-bcfd-39a287df351b" />


### Выводы
1. Подкласс ElectricCar наследует от Car и добавляет новый атрибут battery_capacity.
2. Конструктор ElectricCar вызывает конструктор базового класса с помощью super().init().
3. Метод charge() выводит информацию о заряде батареи электромобиля.

## Лабораторная работа №4
### Реализуйте инкапсуляцию для класса, созданного в первом задании. Создайте защищенный атрибут производителя и приватный атрибут модели. Вызовите защищенный атрибут и заставьте машину поехать. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями и получившийся вывод в консоль.

```python
# Определяем класс Car
class Car:
    # Конструктор класса Car
    def __init__(self, make, model):
        # Инициализируем make и model
        self._make = make # Защищённый атрибут
        self.__model = model # Приватный атрибут

    def drive(self):
        print(f"Driving the {self._make} {self.__model}")
# Создаем объект my_car типа Car
my_car = Car("Toyota", "Corolla") # Создание нового экземпляра класса Car
print(my_car._make)
my_car.drive()

# Определяем подкласс ElectricCar, наследующий от Car
class ElectricCar(Car):
    # Конструктор ElectricCar с дополнительным параметром battery_capacity
    def __init__(self, make, model, battery_capacity):
        # Вызываем конструктор базового класса Car
        super().__init__(make, model)
        self.battery_capacity = battery_capacity

    # Метод для заряда батареи электромобиля
    def charge(self):
        print(f"Charging the {self.make} {self.model} with {self.battery_capacity} kWh")

# Создаем объект my_electric_car типа ElectricCar
my_electric_car = ElectricCar("Tesla", "Model S", 75)

# Вызываем методы для электромобиля
my_electric_car.drive()
my_electric_car.charge()
```

### Результат.
<img width="846" height="819" alt="image" src="https://github.com/user-attachments/assets/03ae49ad-a062-44b7-8f8d-674d51f45ac4" />
<img width="1271" height="152" alt="image" src="https://github.com/user-attachments/assets/9b7f11b8-8768-4668-9414-4ef10034d6da" />

### Выводы
1. Попытка прямого доступа к приватному атрибуту model внутри класса вызывает AttributeError.

## Лабораторная работа №5
### Реализуйте полиморфизм создав основной (общий) класс “Shape”, а также еще два класса “Rectangle” и “Circle”. Внутри последних двух классов реализуйте методы для подсчета площади фигуры. После этого создайте массив с фигурами, поместите туда круг и прямоугольник, затем при помощи цикла выведите их площади. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями и получившийся вывод в консоль

```python
class Shape:
    def area(self):
        pass
class Rectangle(Shape):
    def __init__(self, width, height):
        self.width = width
        self.height = height
    def area(self):
        return self.width * self.height
class Circle(Shape):
    def __init__(self, radius):
        self.radius = radius
    def area(self):
        return 3.14 * self.radius * self.radius
shapes = [Rectangle(5, 10), Circle(2)]
for shape in shapes:
    area = shape.area()
    print(f"Площадь {type(shape).__name__} равна {area}")
```

### Результат.
<img width="544" height="844" alt="image" src="https://github.com/user-attachments/assets/ba4ca906-cec8-4f05-a911-ee5dd00f5636" />

### Выводы
1. Наследование: `Rectangle` и `Circle` наследуют от `Shape`.
2. Полиморфизм: Метод `area()` определен в базовом классе и переопределен в подклассах.

## Самостоятельная работа №1
### Самостоятельно создайте класс и его объект. Они должны отличаться, от тех, что указаны в теоретическом материале (методичке) и лабораторных заданиях. Результатом выполнения задания будет листинг кода и получившийся вывод консоли.

```python
class Coffee:
    def __init__(self, name, brand):
        self.name = name
        self.brand = brand

    def ThisCoffee(self):
        print(f"Можно {self.name}, пожалуйста, {self.brand}! ")

MyCoffee = Coffee("фильтр", "Эфиопию")
MyCoffee.ThisCoffee()
```
### Результат.
<img width="538" height="160" alt="image" src="https://github.com/user-attachments/assets/5ccac463-06e8-421e-b36c-d54fd0dbff5f" />


### Выводы
1. Конструктор init() инициализирует эти атрибуты при создании объекта.
2. Метод ThisCoffee() выводит сообщение о возможности употребления кофе конкретного сорта и бренда.
3. Создан объект MyCoffee типа Coffee с параметрами ("фильтр", "Эфиопию").

## Самостоятельная работа №2
### Самостоятельно создайте атрибуты и методы для ранее созданного класса. Они должны отличаться, от тех, что указаны в теоретическом материале (методичке) и лабораторных заданиях. Результатом выполнения задания будет листинг кода и получившийся вывод консоли.

```python
class Coffee:
    def __init__(self, name, brand, vkus, price):
        self.name = name
        self.brand = brand
        self.vkus = vkus
        self.price = price
    def ThisCoffee(self):
        print(f"Можно {self.name}, пожалуйста, {self.brand}!")

    def Info(self):
        print(f" Ого, это же {self.vkus} обработка, а стоит сколько? {self.price}? Конечно беру!")

myCoffee = Coffee("фильтр", "Эфиопию", "анаэробная", "249 рублей")
myCoffee.ThisCoffee()
myCoffee.Info()
```
### Результат.
<img width="654" height="166" alt="image" src="https://github.com/user-attachments/assets/f41971f1-2311-4d33-a5c2-6db319e2d463" />

### Выводы
1. Метод Info() выводит информацию о вкусе и цене кофе.
2. Создан объект myCoffee типа Coffee с параметрами ("фильтр", "Эфиопии", "анаэробная", "249 рублей").
3. Вызваны методы ThisCoffee() и Info() для вывода информации о кофе

## Самостоятельная работа №3
### Самостоятельно реализуйте наследование, продолжая работать с ранее созданным классом. Оно должно отличаться, от того, что указано в теоретическом материале (методичке) и лабораторных заданиях. Результатом выполнения задания будет листинг кода и получившийся вывод консоли.

```python
class Coffee:
    def __init__(self, name, brand, vkus, price):
        self.name = name
        self.brand = brand
        self.vkus = vkus
        self.price = price
    def ThisCoffee(self):
        print(f"Можно {self.name}, пожалуйста, {self.brand}!")

    def Info(self):
        print(f" Ого, это же {self.vkus} обработка, а стоит сколько? {self.price}? Конечно беру!")

class Tea(Coffee):
    def __init__(self, name, brand, vkus, price, obzjarka):
        super().__init__(name, brand, vkus, price)
        self.obzjarka = obzjarka
    def whatObzjarka(self):
        print(self.obzjarka)

myCoffee = Coffee("фильтр", "Эфиопию", "анаэробная", "249 рублей")
myCoffee.ThisCoffee()
myCoffee.Info()

myTea = Tea("чай", "чёрный", "персиковый", "129 рублей", "листовой")
myTea.ThisCoffee()
myTea.whatObzjarka()
```
### Результат.
<img width="650" height="199" alt="image" src="https://github.com/user-attachments/assets/69adf649-8e9e-468a-b24d-01e0c8105980" />

### Выводы
1. Вызывается конструктор базового класса с помощью super().init().
2. Добавлен новый метод whatObzjarka().

## Самостоятельная работа №4
### Самостоятельно реализуйте инкапсуляцию, продолжая работать с ранее созданным классом. Она должна отличаться, от того, что указана в теоретическом материале (методичке) и лабораторных заданиях. Результатом выполнения задания будет листинг кода и получившийся вывод консоли.

```python
class Coffee:
    def __init__(self, name, brand, vkus, price):
        self.name = name
        self.brand = brand
        self.vkus = vkus
        self.price = price
    def ThisCoffee(self):
        print(f"Можно {self.name}, пожалуйста, {self.brand}!")

    def Info(self):
        print(f" Ого, это же {self.vkus} обработка, а стоит сколько? {self.price}? Конечно беру!")

class Tea(Coffee):
    def __init__(self, name, brand, vkus, price, obzjarka):
        super().__init__(name, brand, vkus, price)
        self.obzjarka = obzjarka
    def whatObzjarka(self):
        print(self.obzjarka)

myCoffee = Coffee("фильтр", "Эфиопию", "анаэробная", "249 рублей")
myCoffee.ThisCoffee()
myCoffee.Info()

myTea = Tea("чай", "чёрный", "персиковый", "129 рублей", "листовой")
myTea.ThisCoffee()
myTea.whatObzjarka()

print(f"А вообще я больше люблю {myCoffee.name}")
print(f"Да, это просто кофе, {myTea.brand}")
```

### Результат.
<img width="651" height="195" alt="image" src="https://github.com/user-attachments/assets/28cdbd45-286c-4e08-914e-12179238d47c" />

### Выводы
1. Метод whatObzjarka() определен только в классе Tea.
2. Атрибуты доступны через переменные, что демонстрирует принцип инкапсуляции.

## Самостоятельная работа №5
### Самостоятельно реализуйте полиморфизм. Он должен отличаться, от того, что указан в теоретическом материале (методичке) и лабораторных заданиях. Результатом выполнения задания будет листинг кода и получившийся вывод консоли.
```python
class Kotiki:
    def __init__(self, name):
        self.name = name
    def deistvie(self):
        pass
class Siba(Kotiki):
    def deistvie(self):
        print (f"{self.name}, по ушам получишь!")
class Syoma(Kotiki):
    def deistvie(self):
        print(f"{self.name}, хороший мальчик!")
siba = Siba("Сиба")
syoma = Syoma("Сёма")
siba.deistvie()
syoma.deistvie()
```

### Результат.
<img width="546" height="177" alt="image" src="https://github.com/user-attachments/assets/e68a54a6-9cdd-43a8-8b70-376ae2645fe7" />

### Выводы
1. Классы Siba и Syoma наследуют от Kotiki.
2. Метод deistvie() имеет разные реализации в разных классах.


## Общие выводы по теме
На основе всех представленных выше заданий можно сделать выводы о том, что основные концепты ООП, включают: наследование, полиморфизм и инкапсуляцию, показывая их применение в реальных сценариях программирования.
